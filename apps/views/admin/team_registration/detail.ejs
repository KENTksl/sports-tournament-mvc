<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partical/headercomponent'); %>
</head>

<body>
    <div class="container-fluid position-relative bg-white d-flex p-0">
        <!-- Spinner Start -->
        <div id="spinner" class="show bg-white position-fixed translate-middle w-100 vh-100 top-50 start-50 d-flex align-items-center justify-content-center">
            <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
                <span class="sr-only">Loading...</span>
            </div>
        </div>
        <!-- Spinner End -->

        <!-- Sidebar Start -->
        <%- include('../partical/menucomponent'); %>
        <!-- Sidebar End -->

        <!-- Content Start -->
        <div class="content">
            <!-- Navbar Start -->
            <%- include('../partical/navbarcomponent'); %>
            <!-- Navbar End -->

            <!-- Team Registration Detail Start -->
            <div class="container-fluid pt-4 px-4">
                <div class="row g-4">
                    <div class="col-12">
                        <div class="bg-light rounded h-100 p-4">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h6 class="mb-0">Chi Tiết Hồ Sơ: <%= registration.teamName %></h6>
                                <a href="/admin/team-registration" class="btn btn-sm btn-secondary">Quay lại</a>
                            </div>

                            <div class="row mb-4">
                                <div class="col-md-4 text-center">
                                    <img src="/static/uploads/avatars/<%= registration.logo %>" class="img-fluid rounded-circle mb-3" style="width: 150px; height: 150px; object-fit: cover;">
                                    <h4><%= registration.teamName %></h4>
                                    <% if (registration.tournamentId) { %>
                                        <div class="mb-2">
                                            <span class="badge bg-info text-dark">Đăng ký giải: <%= registration.tournamentId.name || 'Unknown' %></span>
                                        </div>
                                    <% } %>
                                    <div class="mb-3">
                                        <% if(registration.status === 'pending') { %>
                                            <span class="badge bg-warning text-dark">Đang chờ duyệt</span>
                                        <% } else if(registration.status === 'approved') { %>
                                            <span class="badge bg-success">Đã được duyệt</span>
                                            <div class="small text-muted mt-1">Vào lúc: <%= new Date(registration.approvedAt).toLocaleString('vi-VN') %></div>
                                        <% } else { %>
                                            <span class="badge bg-danger">Đã từ chối</span>
                                        <% } %>
                                    </div>
                                    
                                    <% if(registration.status === 'pending') { %>
                                        <div class="d-grid gap-2">
                                            <form action="/admin/team-registration/<%= registration._id %>/approve" method="POST">
                                                <button type="submit" class="btn btn-success w-100 mb-2">
                                                    <i class="fa fa-check me-2"></i>Duyệt Hồ Sơ
                                                </button>
                                            </form>
                                            <form action="/admin/team-registration/<%= registration._id %>/reject" method="POST">
                                                <button type="submit" class="btn btn-danger w-100">
                                                    <i class="fa fa-times me-2"></i>Từ Chối
                                                </button>
                                            </form>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="col-md-8">
                                    <div class="card mb-4">
                                        <div class="card-header">Thông Tin Liên Hệ</div>
                                        <div class="card-body">
                                            <p><strong>Người đại diện:</strong> <%= registration.representative %></p>
                                            <p><strong>Số điện thoại:</strong> <%= registration.phone %></p>
                                            <p><strong>Email:</strong> <%= registration.email %></p>
                                            <p><strong>Giới thiệu:</strong> <%= registration.description %></p>
                                        </div>
                                    </div>

                                    <div class="card">
                                        <div class="card-header d-flex justify-content-between">
                                            <span>Danh Sách Thành Viên (<%= registration.members.length %>)</span>
                                        </div>
                                        <div class="card-body p-0">
                                            <table class="table table-striped mb-0">
                                                <thead>
                                                    <tr>
                                                        <th>#</th>
                                                        <th>Avatar</th>
                                                        <th>Họ Tên</th>
                                                        <th>Số Áo</th>
                                                        <th>CCCD/CMND</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% registration.members.forEach(function(member, index) { %>
                                                        <tr>
                                                            <td><%= index + 1 %></td>
                                                            <td>
                                                                <img src="/static/uploads/avatars/<%= member.avatar %>" style="width: 40px; height: 40px; object-fit: cover;" class="rounded-circle">
                                                            </td>
                                                            <td><%= member.name %></td>
                                                            <td><%= member.number %></td>
                                                            <td>
                                                                <% if (member.citizenIdImage) { %>
                                                                    <a href="/static/uploads/avatars/<%= member.citizenIdImage %>" target="_blank">
                                                                        <img src="/static/uploads/avatars/<%= member.citizenIdImage %>" style="width: 60px; height: 40px; object-fit: cover;" class="rounded">
                                                                    </a>
                                                                <% } else { %>
                                                                    <span class="text-muted">Không có ảnh</span>
                                                                <% } %>
                                                            </td>
                                                        </tr>
                                                    <% }); %>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Team Registration Detail End -->

            <!-- Footer Start -->
            <div class="container-fluid pt-4 px-4">
                <div class="bg-light rounded-top p-4">
                    <div class="row">
                        <div class="col-12 col-sm-6 text-center text-sm-start">
                            &copy; <a href="#">SPORT TOURNAMENT</a>, All Right Reserved. 
                        </div>
                        <div class="col-12 col-sm-6 text-center text-sm-end">
                            Designed By <a href="https://htmlcodex.com">HTML Codex</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Footer End -->
        </div>
        <!-- Content End -->

        <!-- Back to Top -->
        <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    </div>

    <!-- Error Modal -->
    <% if (locals.error) { %>
        <div class="modal fade" id="errorModal" tabindex="-1" aria-labelledby="errorModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header bg-danger text-white">
                        <h5 class="modal-title" id="errorModalLabel"><i class="fa fa-exclamation-triangle me-2"></i>Thông báo</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body text-center py-4">
                        <% if (error.includes('Tournament is full') || error.includes('full')) { %>
                            <p class="fs-5 text-danger mb-0">Hiện tại giải đã full đội. Hãy xóa các đội trước đó để thêm đội mới.</p>
                        <% } else { %>
                            <p class="fs-5 text-danger mb-0"><%= error %></p>
                        <% } %>
                    </div>
                    <div class="modal-footer justify-content-center">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
                        <% if (registration && registration.tournamentId) { %>
                            <a href="/admin/football/tournament/detail/<%= registration.tournamentId._id || registration.tournamentId %>" class="btn btn-primary">Đến trang giải đấu</a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                var errorModal = new bootstrap.Modal(document.getElementById('errorModal'));
                errorModal.show();
            });
        </script>
    <% } %>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/static/admin/lib/chart/chart.min.js"></script>
    <script src="/static/admin/lib/easing/easing.min.js"></script>
    <script src="/static/admin/lib/waypoints/waypoints.min.js"></script>
    <script src="/static/admin/lib/owlcarousel/owl.carousel.min.js"></script>
    <script src="/static/admin/lib/tempusdominus/js/moment.min.js"></script>
    <script src="/static/admin/lib/tempusdominus/js/moment-timezone.min.js"></script>
    <script src="/static/admin/lib/tempusdominus/js/tempusdominus-bootstrap-4.min.js"></script>

    <!-- Template Javascript -->
    <script src="/static/admin/js/main.js"></script>
    <%- include('../partical/footercomponent'); %>
</body>

</html>